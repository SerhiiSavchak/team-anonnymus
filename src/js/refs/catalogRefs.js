const ulRef = document.querySelector('.catalog-list');
const searchForm = document.querySelector('.catalog-form');
const searchInput = document.querySelector('.catalog-form-input');
const errorContainer = document.querySelector('.catalog-error-container');
const btnClear = document.querySelector('.catalog-clear-btn');
const containerTui = document.getElementById('tui-pagination-container');

export {
  ulRef,
  searchForm,
  searchInput,
  errorContainer,
  btnClear,
  containerTui,
};
